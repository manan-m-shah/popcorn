import { State } from "../types/Context";

const initialState: State = {
    account: null,
    movies: null,
    stakeModal: false,
    addMovie: false,
    activeMovie: 0,
}

export default initialState